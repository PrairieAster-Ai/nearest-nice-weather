{
  "name": "@nearest-nice-weather/api",
  "version": "1.0.0",
  "description": "Weather Intelligence API - Serverless Functions",
  "main": "index.js",
  "scripts": {
    "start": "vercel dev --listen 4000",
    "build": "echo 'No build needed for serverless functions'",
    "test": "echo 'API tests integrated with CI/CD pipeline'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@vercel/node": "^2.3.0",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "@types/pg": "^8.15.5",
    "typescript": "^5.9.2"
  }
}
